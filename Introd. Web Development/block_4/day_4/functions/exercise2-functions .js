// function greaterIndex(numbers) {
//     return Math.max.apply(numbers)
// }

function greaterIndex(numbers) {
    let index = 0;
    let greater = 0;
    for (let i = 0; i <= numbers.length; i++ ) {
        
    }
}